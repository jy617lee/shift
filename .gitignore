# OS / 편집기
.DS_Store
.idea/

# 개인 토큰 포함 — git 추적 제외
.mcp.json
.claude/settings.local.json

# 로컬 전용 에셋 (git 비추적)
design/
test_img/
4*2widgetdesign.png

# Android 루트 빌드 잔재 (모노레포 전환 후 android/ 내부로 이동됨)
local.properties
.gradle/
.kotlin/
build/
